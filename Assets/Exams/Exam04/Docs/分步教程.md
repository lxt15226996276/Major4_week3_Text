# Exam04 分步教程 — 登录注册 + Loading + 移动动画战斗

> **教练宪法**：[`../../Docs/教学规范.md`](../../Docs/教学规范.md) **v7.16.8 · §11.31 四门禁** · [`../../Docs/Coach合规四门禁.md`](../../Docs/Coach合规四门禁.md) · [`../../../Docs/02-学员档案/踩坑记忆库_lixiaotong.md`](../../../Docs/02-学员档案/踩坑记忆库_lixiaotong.md)  
> **清单 + P-L1**：[`教程.md`](教程.md) §一点五 **v3**  
> **档案**：[`学员能力档案_lixiaotong.md`](../../../Docs/02-学员档案/学员能力档案_lixiaotong.md)  
> **开套 P-Score 84** · **S2-** · **递进读码**：Exam02 Auth · Exam06 Loading · Exam03 战斗

> 📋 **Coach 输出前（R1 · §11.28）**：总 Doc → 本文件 **当前大步全文** → **10 项对照**（含 §11.29）→ 再发 Chat。

---

## 使用说明

| 规则 | 说明 |
|------|------|
| **§11.29** | 同域第二套题 · P-L1 **必须含递进条** · 禁止只写「≥ Exam02」 |
| **§11.30** | 文首 **本套命名约定表** · 行业 D2 · **本套内统一** · 档位匹配 S 轨 |
| **大步** | Chat「进行下一步」= **一整步**（共 **6 步**） |
| **小分段** | `### N.1`… 在本文件 + **同一条 Chat** |
| **A+B** | 每大步 **📌A + 🔷B**；脚本步 **§11.9 三块** |
| **Chat 改库 K1** | 「进行下一步」= **只教 · 不改仓库** |

---

## 同类题递进对照表（§11.29 · v3 · 2026-07-03）

| 功能域 | 上套 P-L1（已有 · 底线） | **Exam04 P-L1 新增** | P+1 |
|--------|--------------------------|------------------------|-----|
| Auth | `ShowPanel` · Dictionary · OnDestroy | **`SceneNames`** · **Exam04 `AccountData`** · **Trim** · →Loading | 接口拆分 |
| Loading | Start 协程 · 禁 OnEnable-only | **5s 假进度** · **StopCoroutine 对称** | Min+Async 注释 |
| 移动 | `Space.Self` | **`SetBool("IsMoving")`** · 敌人 Idle | CC / BlendTree |
| 战斗 | Instantiate · 开火 · OnDestroy | **注入禁 Find** · **`aliveCount` 全灭** | BulletPool |
| UI | mute | **双反馈** Text/Sprite | Addressables |
| 规范 | 部分 §11.19 | **全脚本五层** · **本套命名约定 N1** | 导图 png |

---

## 本套命名约定（§11.30 · 档位 **N1** · S2- · P 84）

> **原则**：对照 **行业常见 C# / Unity 习惯** · **本套内统一** · 相对 Exam02 **只递进 1～2 条/域**（可消化）

| 类别 | Exam04 本套约定 | 行业依据 | 相对 Exam02/03 |
|------|-------------------|----------|----------------|
| **namespace** | `Exam.Exam04` | 工程隔离 · U16 | 继承 |
| **私有非序列化字段** | **`_camelCase`**（如 `_accounts`） | MS / 客户端常见私有字段前缀 | **递进** · Exam02 `accountDic` |
| **`[SerializeField]` 私有** | **camelCase**（如 `btnLogin`） | 与 Inspector 一致 · 可读 | 继承 Exam02 拖引用习惯 |
| **类型 / public 成员** | **PascalCase** | C# 标准 | 继承 |
| **private 方法** | **PascalCase** | Unity C# 主流 | 继承 |
| **局部变量 / 参数** | **camelCase** | C# 标准 | 继承 |
| **常量 / 静态类** | **PascalCase**（`SceneNames`） | 禁魔法字符串 | **本套新增** |
| **Animator 参数** | 字符串用 **`nameof` 或 const** · 本卷可用 `"IsMoving"` | 防拼写错 | 本套新增 |
| **Log 安全** | **不写明文密码** | 手游/上线前审查 | **递进** · Exam02 Log 含密码 |
| **禁止本套 P-L1** | 接口 `I*` 全套 · `m_` 与 `_` 混用 · 同一类字段有的 `_` 有的无 | 超 N1 档 | P+1 / 阶段 02 |

**验收**：第 6 步对照本表 **抽检 3 个脚本** · 不一致 = **D2 P-L1 小项待补**（不挡 L1 若仅命名风格）。

---

## 本套 P+1 总表（v3 · 已剔除升格进 P-L1 的项）

| # | P+1 | 维度 | ☐ |
|:-:|-----|------|:-:|
| 1 | Loading 注释 **Min(fake,real/0.9f)** 与 Exam05/06 差异 | D4 | ☐ |
| 2 | **CharacterController** 替代 Translate | U11 | ☐ |
| 3 | **BlendTree** 八向（W3 Lab 预习） | K2 | ☐ |
| 4 | **BulletPool** 对象池 | D6 | ☐ |
| 5 | Build 断网 · `Exam04_思维导图.png` | D6 | ☐ |

**已升格 P-L1（不算 P+1）**：AccountData · SceneNames · SetBool · 注入 · aliveCount · §11.19 全脚本 · mute 双反馈

---

## 大步与试卷对照表

| 大步 | 试卷分点 | 小分段 | 状态 |
|:----:|----------|--------|:----:|
| **第 1 步** | 1 · 工程 | 1.1 读题递进 · 1.2 三场景+Build | ✅ |
| **第 2 步** | 2 · Auth | 2.1 Prefab · 2.2 三脚本 §11.9 | ✅ |
| **第 3 步** | 3 · Loading | 3.0 Prefab · LoadingController | ✅ |
| **第 4 步** | 4～5 · 移动动画 | **4.0 Animator 读码** · PlayerController · GameManager | **🔒 ← 当前** |
| 第 4 步 | 4～5 · 移动动画 | 4.1 Main 场景 · 4.2 Player+Animator+刷怪 | 🔒 |
| 第 5 步 | 6～9 · 战斗 | 5.1 BattleUI · 5.2 子弹+GameManager | 🔒 |
| 第 6 步 | 10 · 交付 | 6.1 注释 · 6.2 Build 断网 | 🔒 |

---

## 大步路线图

```
第1步  读题+导图(3递进点)+三场景+Build 0→1→2
  ↓
第2步  LoginUI Prefab · AccountData · AuthController · SceneNames
  ↓
第3步  **复用 LoadingCanvas.prefab** · Unpack · LoadingController 5s → Main
  ↓
第4步  Player WASD+SetBool · 3敌 · 敌人Idle
  ↓
第5步  音乐/开火/关闭 · Bullet · aliveCount 胜利
  ↓
第6步  §11.19 全脚本 · Build 断网 · 交付
```

---

## 第 1 步：读题 + 工程与三场景（考点 1）

> 状态：**🔒 待学员完成** · **← 当前**  
> **小分段**：1.1 读题·递进导图 · 1.2 三场景+Build

### 1.1 读题 · 递进思维导图

| # | 操作 |
|:-:|------|
| 1 | 通读 [`教程.md`](教程.md) §一 + **§一点五 v3** |
| 2 | 画导图：**Login → Loading 5s → Main**（动画+三钮+全灭） |
| 3 | 导图**必须标注 3 个递进点**（相对 Exam02/03）：Auth 串联 Loading · 5s 假进度 · 动画+aliveCount |
| 4 | 草图即可 · 第 6 步导出 `Exam04_思维导图.png` |

**与 Exam02/03/06 差异（口试）**

| 项 | 上套 | **Exam04** |
|----|------|------------|
| 场景 | 2～5 个 | **3** 固定链路 |
| Loading | Async/无/6s | **5s 假进度** |
| 战斗胜负 | 加血/扣血/无 | **敌人全灭计数** |
| 动画 | 无 | **IsMoving + 敌人 Idle** |

### 1.2 三场景 + Build

| # | 操作 | 验收 |
|:-:|------|------|
| 1 | 建 `Scenes/Exam04_LoginScene.unity` | 可打开 |
| 2 | 建 `Scenes/Exam04_LoadingScene.unity` | 可打开 |
| 3 | 建 `Scenes/Exam04_MainScene.unity` | 可打开 |
| 4 | Build Settings：**0 Login · 1 Loading · 2 Main** | Exam04 **置顶** |
| 5 | 建空目录 `Scripts/`（本步仍不写脚本） | 存在即可 |

**Hierarchy（本步空场景）**

```
Exam04_LoginScene          Exam04_LoadingScene       Exam04_MainScene
├── Main Camera            ├── Main Camera           ├── Main Camera
└── Directional Light      └── Directional Light     └── Directional Light
```

### 🎯 L1 / P-L1 / P+1（本大步）

| 档 | 要求 |
|----|------|
| **L1** | 三 `.unity` · Build 0→1→2 |
| **P-L1** | 导图含 **3 递进点** · 能口述 **vs Exam02/03** 各 1 条 |
| **P+1** | 导图加 **四套对比小表**（02/03/06/04） |

### 本步验收

- [ ] 三场景存在  
- [ ] Build **0=Login 1=Loading 2=Main** · **Index 0 不是 Exam01/03**  
- [ ] 导图标注：**SceneNames 规划 · 5s Loading · 动画+全灭**

### 📌 原理加餐（A）

> **U16** · **U03**

- **是什么**：Exam04 把 Exam02 **Auth 链**、Exam06 **协程 Loading**、Exam03 **战斗 Instantiate** 合成 **一条三段式流水线**。  
- **卷面为何**：考点 3 强制独立 Loading 场景；考点 5 强制 Animator。  
- **再挖一层**：Build Index 0 必须是 Login — 否则打包后跳过注册（踩坑 L2 同类）。

### 🔷 工程进阶 B

**FICM · 工程骨架**

| 维 | 试卷 L1 | 你现状 | **Exam04 递进** |
|:--:|---------|--------|-----------------|
| ① 职责 | 三场景文件 | Exam03 做过 2 场景 | **+Loading 中间场景** |
| ② 数据 | — | Exam02 AccountData | 第 2 步 **完整敲 AccountData 范本**（非复制偷懒） |
| ⑤ 状态 | Build 顺序 | 踩坑 L2 | **断网从 Index 0 测** |
| ⑧ 可维护 | 场景名 | Exam02 字符串散落 | 第 2 步 **`SceneNames` 类** |

**B本步 · 对照代码**（第 2 步创建 · 本步先记名）

```csharp
namespace Exam.Exam04
{
    public static class SceneNames
    {
        public const string Login = "Exam04_LoginScene";
        public const string Loading = "Exam04_LoadingScene";
        public const string Main = "Exam04_MainScene";
    }
}
```

### 进度（第 1 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 2 步：登录注册（考点 2）· v7.16.6 重生成

> 状态：**🔒 待学员完成** · **← 当前**  
> **§11.29**：功能递进 · **§11.30 命名档位 N1** · 本步 **3 脚本 × §11.9 三块**  
> **禁止**：「复制 Exam02 改 namespace」· Chat/讲义 **每个脚本须有完整带注释范本**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| **2.1** | Prefab 体检 · 场景 · Inspector | 两 Panel 互斥 · 无 Missing |
| **2.2.1** | `SceneNames.cs` | 静态类 · 三常量 |
| **2.2.2** | `AccountData.cs` | 单例 Dictionary · `_accounts` |
| **2.2.3** | `AuthController.cs` | ShowPanel · →Loading · OnDestroy |

### 本步递进 + 命名（对照表 · 开做前读）

| 项 | [继承 §七] | [递进 vs Exam02] | [命名 N1 · §11.30] |
|----|------------|------------------|---------------------|
| 面板 | ShowPanel 互斥 | 两 Panel（无 Initial） | — |
| 数据 | Dictionary · Trim | Log **不写密码** | `_accounts` 非 `accountDic` |
| 跳转 | LoadScene | **`SceneNames.Loading`** | 类名 **`SceneNames`** · 文件 `SceneNames.cs` |
| UI 字段 | OnDestroy 解绑 | Auth 侧 Trim | SerializeField **`btnLogin`** camelCase |
| 工程 | namespace | Exam04 隔离 | **全部** `Exam.Exam04` |

---

### Prefab 体检（§11.13 · O1d）

| 物体/组件 | Prefab 现状 | 问题 | Exam04 改法 |
|-----------|-------------|------|-------------|
| `LoginUI` 根 | Canvas Scale=0 | 有 CanvasScaler · 正常 | 保留 |
| 根脚本 | 旧 LoginUI 脚本 | Missing / 错场景名 | **Remove** · 逻辑改 **AuthManager** |
| `LoginPanel` | **Active** | — | 入口 · 默认 **开** |
| `RegisterPanel` | Prefab 默认 **关** | — | 保持 **关** |
| `LoginButton` | 在 LoginPanel | — | → `btnLogin` |
| `BtnRegister` | 在 LoginPanel | 易与 RegisterButton 混 | → `btnRegister` **打开注册页** |
| `RegisterButton` | 在 **RegisterPanel** | 与 BtnRegister 不同 | → `btnConfirmRegister` **确认注册** |
| `BtnBackRegister` | 在 RegisterPanel | — | → `btnBackRegister` |
| `BtnExit` | 在 LoginPanel | — | → `btnExit` |
| 密码 Input ×2 | ContentType | 可能 Standard | **Password** |
| `CreatRoleUI.prefab` | 存在 | 本卷不用 | 不拖 |

**Hierarchy（LoginScene 目标）**

```
Exam04_LoginScene
├── Main Camera
├── Directional Light
├── EventSystem
├── AuthManager                    ← 挂 AuthController.cs
│   └── AccountData                ← 子物体 · 挂 AccountData.cs
└── LoginUI (Prefab)
    └── Background
        ├── LoginPanel             ← Active · LoginButton / BtnRegister / BtnExit / Input×2
        └── RegisterPanel          ← 默认 Inactive · RegisterButton / BtnBackRegister / Input×2
```

---

### 2.1 场景操作

| # | 操作 |
|:-:|------|
| 1 | 打开 `Exam04_LoginScene` |
| 2 | 拖 `Prefabs/LoginUI.prefab` |
| 3 | 无 EventSystem → **UI → Event System** |
| 4 | 空物体 **`AuthManager`** · 挂 **`AuthController.cs`** |
| 5 | 其子物体 **`AccountData`** · 挂 **`AccountData.cs`** |
| 6 | Prefab 根 **Remove** 旧/Missing 脚本 |
| 7 | **Build Settings** 含三场景且 **Loading=Index 1** |

#### `LoginUI` · Canvas Scaler

| 字段 | 设置值 | 含义 |
|------|--------|------|
| UI Scale Mode | Scale With Screen Size | 分辨率自适应 |
| Reference Resolution | **1920 × 1080** | 与 Loading/Main 一致 |
| Match | **0.5** | 宽高分屏折中 |

#### `PasswordInput` ×2（LoginPanel / RegisterPanel 各一）

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Content Type | **Password** | 输入掩码 · 卷面登录规范 |

---

### 2.2.1 `SceneNames.cs`（静态类 · 无挂载）

#### 本段思路

LoadScene 的字符串 **只许** 从本类读取；改场景文件名时 **只改这一处**（§11.29 本套新增 · 禁魔法字符串）。

#### 带注释 P-L1 范本

```csharp
namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 场景名常量。LoadScene 禁止散落字符串。
    /// [本套新增] Exam02 用字面量；本套统一 const 类。
    /// </summary>
    public static class SceneNames
    {
        public const string Login = "Exam04_LoginScene";
        public const string Loading = "Exam04_LoadingScene";
        public const string Main = "Exam04_MainScene";
    }
}
```

> **命名 N1**：类名 **`SceneNames`**（复数）· 文件 **`SceneNames.cs`** · 与 `SceneName` 单数二选一但 **本套统一 SceneNames**。

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `static class` | 无 MonoBehaviour · 纯常量容器 |
| 三常量 | 字符串 **= 场景文件名**（不含 `.unity`） |
| `Loading` | Auth 成功跳转目标 · 第 3 步协程再进 `Main` |

---

### 2.2.2 `AccountData.cs`（挂 `AuthManager/AccountData`）

#### 本段思路

1. **Awake**：单例 + 重复实例 Destroy + **return** + `DontDestroyOnLoad`。  
2. **Register**：Trim → 判空 → 判重复 → `_accounts.Add`。  
3. **TryLogin**：Trim → `TryGetValue` 比对（P-L1 用 **bool** 返回即可）。

#### 带注释 P-L1 范本

```csharp
using System.Collections.Generic;
using UnityEngine;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 账号字典：键=账号，值=密码；跨 Loading/Main 不丢失。
    /// [继承 §七] 单例 + Dictionary + Trim + TryGetValue。
    /// </summary>
    public class AccountData : MonoBehaviour
    {
        // --- 单例 ---
        // [命名递进 · D2] public 属性 private set；外部只读 Instance 引用
        public static AccountData Instance { get; private set; }

        // --- 数据 ---
        // [命名递进 · D2] 私有非序列化字段 _camelCase；勿写 _accoutDic 拼写错
        private readonly Dictionary<string, string> _accounts = new Dictionary<string, string>();

        private void Awake()
        {
            if (Instance != null && Instance != this)
            {
                Destroy(gameObject);
                return; // 重复实例必须 return，否则下面仍会覆盖 Instance
            }
            Instance = this;
            DontDestroyOnLoad(gameObject); // 进 Loading/Main 后字典仍在
        }

        /// <summary>
        /// 思路：Trim → 判空 → 判重复 → Add → out message。
        /// [递进 vs Exam02] message 不写明文密码。
        /// </summary>
        public bool Register(string account, string password, out string message)
        {
            account = account?.Trim();
            password = password?.Trim();
            message = string.Empty;

            if (string.IsNullOrEmpty(account) || string.IsNullOrEmpty(password))
            {
                message = "账号或密码不能为空";
                return false;
            }
            if (_accounts.ContainsKey(account))
            {
                message = "账号已存在";
                return false;
            }

            _accounts.Add(account, password);
            message = $"[注册成功] account={account}，当前注册总数：{_accounts.Count}";
            return true;
        }

        /// <summary>思路：Trim → TryGetValue → 比对 stored 与 password。</summary>
        public bool TryLogin(string account, string password)
        {
            account = account?.Trim();
            password = password?.Trim();
            return _accounts.TryGetValue(account, out string stored) && stored == password;
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `{ get; private set; }` | **[命名 N1]** 比 `public static Instance;` 更安全 |
| `_accounts` | **[命名 N1]** · 勿 `_accoutDic` |
| Awake `return` | 销毁重复实例后 **必须 return** |
| Register message | **[递进]** 无密码明文 · Exam02 有 |
| `TryLogin` bool | **[继承 §七]** · P+1 可加 `out message` 区分失败原因 |

---

### 2.2.3 `AuthController.cs`（挂 **AuthManager** 物体）

> **物体名** `AuthManager` · **类名** `AuthController`（职责：UI + 跳转，不是数据单例）。

#### 本段思路

1. **Awake** → `ShowPanel(loginPanel)`。  
2. **Start** → 命名方法绑 `onClick`（便于 OnDestroy `RemoveListener`）。  
3. **Register / Login** → Trim → `AccountData` → 成功 **`SceneNames.Loading`**。

#### 带注释 P-L1 范本

```csharp
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 登录 UI：Login/Register 互斥、Dictionary 校验、进 Loading。
    /// [递进] SceneNames.Loading · 两 Panel（Exam02 三 Panel 含 Initial）。
    /// </summary>
    public class AuthController : MonoBehaviour
    {
        // --- UI 面板 ---
        [SerializeField] private GameObject loginPanel;
        [SerializeField] private GameObject registerPanel;

        [Header("Login")]
        [SerializeField] private InputField loginAccountInput;
        [SerializeField] private InputField loginPasswordInput;
        [SerializeField] private Button btnLogin;
        [SerializeField] private Button btnRegister;
        [SerializeField] private Button btnExit;

        [Header("Register")]
        [SerializeField] private InputField registerAccountInput;
        [SerializeField] private InputField registerPasswordInput;
        [SerializeField] private Button btnConfirmRegister;
        [SerializeField] private Button btnBackRegister;

        private void Awake()
        {
            ShowPanel(loginPanel);
        }

        private void Start()
        {
            btnRegister.onClick.AddListener(OnOpenRegister);
            btnBackRegister.onClick.AddListener(OnBackToLogin);
            btnConfirmRegister.onClick.AddListener(OnRegisterClick);
            btnLogin.onClick.AddListener(OnLoginClick);
            btnExit.onClick.AddListener(OnExitClick);
        }

        /// <summary>思路：两 Panel 同一时刻只亮一个。[继承 §七] ShowPanel。</summary>
        private void ShowPanel(GameObject target)
        {
            loginPanel.SetActive(target == loginPanel);
            registerPanel.SetActive(target == registerPanel);
        }

        private void OnOpenRegister() => ShowPanel(registerPanel);
        private void OnBackToLogin() => ShowPanel(loginPanel);
        private void OnExitClick() => Application.Quit();

        private void OnRegisterClick()
        {
            string account = registerAccountInput.text.Trim();
            string password = registerPasswordInput.text.Trim();
            if (AccountData.Instance.Register(account, password, out string msg))
            {
                registerAccountInput.text = string.Empty;
                registerPasswordInput.text = string.Empty;
                ShowPanel(loginPanel);
            }
            Debug.Log(msg);
        }

        private void OnLoginClick()
        {
            string account = loginAccountInput.text.Trim();
            string password = loginPasswordInput.text.Trim();
            if (AccountData.Instance != null && AccountData.Instance.TryLogin(account, password))
            {
                SceneManager.LoadScene(SceneNames.Loading); // [递进] 本卷先进 Loading
                return;
            }
            Debug.Log("登录失败");
            loginAccountInput.text = string.Empty;
            loginPasswordInput.text = string.Empty;
            loginAccountInput.ActivateInputField();
        }

        private void OnDestroy()
        {
            if (btnLogin != null) btnLogin.onClick.RemoveListener(OnLoginClick);
            if (btnRegister != null) btnRegister.onClick.RemoveListener(OnOpenRegister);
            if (btnConfirmRegister != null) btnConfirmRegister.onClick.RemoveListener(OnRegisterClick);
            if (btnBackRegister != null) btnBackRegister.onClick.RemoveListener(OnBackToLogin);
            if (btnExit != null) btnExit.onClick.RemoveListener(OnExitClick);
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `ShowPanel` | **[继承 §七]** · 禁止 ShowLogin/ShowRegister 多方法 |
| `SceneNames.Loading` | **[递进]** · Exam02 直进 Server/Game |
| SerializeField `btnLogin` | **[命名 N1]** camelCase · 与 `_accounts` 私有字段区分 |
| 命名方法 + RemoveListener | **[继承 §七]** |
| `BtnRegister` vs `RegisterButton` | 前者 **开注册页** · 后者 **提交注册** |

#### Inspector 拖引用（AuthController · AuthManager 上）

| SerializeField | 拖 Prefab 内谁 |
|----------------|----------------|
| loginPanel | `LoginPanel` |
| registerPanel | `RegisterPanel` |
| loginAccountInput | LoginPanel → `AccountInput` |
| loginPasswordInput | LoginPanel → `PasswordInput` |
| btnLogin | `LoginButton` |
| btnRegister | LoginPanel → **`BtnRegister`** |
| btnExit | `BtnExit` |
| registerAccountInput | RegisterPanel → `AccountInput` |
| registerPasswordInput | RegisterPanel → `PasswordInput` |
| btnConfirmRegister | RegisterPanel → **`RegisterButton`** |
| btnBackRegister | `BtnBackRegister` |

---

### 🎯 L1 / P-L1 / P+1（本大步）

| 档 | 要求 |
|----|------|
| **L1** | 注册 → 登录 → 进 **Loading 场景** |
| **P-L1** | **SceneNames** · **AccountData** · **ShowPanel** · **Trim** · **OnDestroy** · **命名 N1 表** |
| **P+1** | `TryLogin(out message)` 区分失败原因 · `TryLogin` 错误 Toast UI |

### 本大步验收

**试卷 L1**

- [ ] 注册账号 → 回 LoginPanel → 登录成功  
- [ ] 进入 **`Exam04_LoadingScene`**（可先空场景）

**P-L1 + D2**

- [ ] 三脚本 **`namespace Exam.Exam04`**  
- [ ] **`SceneNames`** 类/文件命名 · LoadScene 用常量  
- [ ] **`_accounts`** · Instance `{ get; private set; }` · Awake 重复 **return**  
- [ ] **无**写死账号密码 · **无** Missing Script  
- [ ] **Password** ContentType · **BtnRegister ≠ RegisterButton** 拖对

### 📌 原理加餐（A）

> **U17** · **U16** · **§11.30**

- **是什么**：登录注册 = **UI 状态机（Panel）+ 数据层（Dictionary）+ 场景流（LoadScene）** 三层分离。  
- **卷面为何**：考点 2 只验注册登录；考点 3 才 Loading — 所以 **Login 成功不许直进 Main**。  
- **再挖一层**：`DontDestroyOnLoad` 只保留 **AccountData 物体**；LoginUI 随 Login 场景卸载 — 故数据 **不能** 写在 AuthController 里。

### 🔷 工程进阶 B

**行业依据** — 手游登录链：UI 控制器 ≠ 账号仓库（吸收库 §六 F01-AUTH）

**FICM · Auth 包（本步）**

| 维 | 试卷 L1 | Exam02 你 | **Exam04 P-L1** |
|:--:|---------|-----------|-----------------|
| ① 职责 | 注册登录 | LoginUIController 大包 | **AuthController UI + AccountData 数据** |
| ② 数据 | Dictionary | accountDic | **`_accounts` + 无密码 Log** |
| ③ UI | 两 Panel | 三 Panel | **两 Panel** |
| ⑤ 状态 | LoadScene | 字面量 | **`SceneNames`** |
| ⑥ 生命周期 | — | OnDestroy 部分 | **全套 RemoveListener** |
| ⑧ 可维护 | 可读 | 拼写 accountDic | **命名约定表 N1** |

**B本步 · 对照要点**

- 数据与 UI 分脚本 = 行业 **Repository + Presenter** 雏形（本卷不拆接口）。  
- **SceneNames** = 阶段 01 的「场景表」最小形态。

**B下一档（P+1）**：`IAccountService` + 错误码 enum · 仍不必上 DI 容器。

### 进度（第 2 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 3 步：Loading 5 秒（考点 3）· v7.16.8 完整重生成

> 状态：**✅ 学员进行中**（L1 可过 · P-L1 小项待补）  
> **§11.31 G1**：UI 大步 **Glob 已有 `LoadingCanvas.prefab`** · **禁止** UI→Canvas 手搭  
> **§11.29 递进**：Exam06 **Start 协程 + L3** → Exam04 **5s 纯假进度 + StopCoroutine 对称**  
> **§11.30 N1**：`namespace Exam.Exam04` · 私有字段 `_loadRoutine` · `[SerializeField]` camelCase

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| **3.0** | **Prefab 候选 + 体检表** | Glob 有源 · 写差量 |
| **3.1** | 拖入 **Unpack** · 差量改 Inspector | Slider 0～1 · 背景可见 |
| **3.2** | `LoadingController.cs` · §11.9 三块 | **5 秒**满 → Main |

#### 本段思路（整步）

Login 成功后进 **独立 Loading 场景** → **复用 Exam06 Prefab 作 UI 底**（G1）→ **5 秒协程刷 Slider** → **`SceneNames.Main`**。相对 Exam06：**去掉 Async/Min/静音**，保留 **Start 协程 + 对称 Stop**。

---

### 3.0 Prefab 候选表（Glob · **O1d 强制**）

> **开写本步前**：`Glob Assets/Exams/**/LoadingCanvas.prefab` · 有则 **第一小节必须是体检 + Unpack**。

| 候选 Prefab | 结论 | 理由 |
|-------------|:----:|------|
| **`Exam06/Prefabs/LoadingCanvas.prefab`** | ✅ **首选** | 你 **Exam06 已做** · Slider+Text+背景+BtnMute 齐 · 结构最完整 |
| **`Exam05/Prefabs/LoadingCanvas.prefab`** | ✅ 备选 | 同为王者风 Loading · 与 Exam06 层级几乎一致 |
| UI → Canvas → Slider **从零手搭** | ❌ **废止** | 违反 **§11.13 O1d** · 仓库已有同类 Prefab |

**递进对照（Loading UI）**

| 维 | Exam06 你现状 | **Exam04 P-L1** |
|:--:|---------------|-----------------|
| UI 来源 | 手搭或 Prefab | **复用 Exam06/05 LoadingCanvas · Unpack 差量改** |
| 时长 | **6s** + Async+Min | **5s 纯假进度**（无 Async） |
| 静音 | BtnMute+BGM | 卷面 **不要求** · Prefab 里 **Disable/删** |
| 协程 | Start 启 · 部分无 Stop | **Start + 存句柄 + OnDestroy StopCoroutine** |

---

### 3.0b Prefab 体检（`Exam06/Prefabs/LoadingCanvas.prefab` · Read YAML）

| 物体/组件 | Prefab 现状 | Layout / 问题 | Exam04 改法 |
|-----------|-------------|---------------|-------------|
| **LoadingCanvas** | Canvas + Scaler + **LoadingPanel** | 1920×1080 王者风 | 拖入 **Unpack Completely** · 根名保持 **LoadingCanvas** |
| **BackGround** | 全屏 Image · `BG.png` | Raycast 可能开 | **Raycast Target 关** · 保留背景 |
| **LoadingPanel** | 中间面板容器 | 含 Slider+Text+RoleImage | 保留 · 子物体按表差量改 |
| **ProgressSlider** | Min/Max **0~1** · Fill+Background | Exam06 6s 逻辑位 | **保留** · **Interactable 关** · Value **0** |
| **TipTextTop** | 上方提示 | Exam06 联动进度 % | 改文案 **「加载中…」** · **Raycast 关** · **P+1** 可脚本刷 `%` |
| **TipTextBottom** | 下方提示 | 卷面未要求 | **Active 关** 或删 |
| **TipProgressText** | Slider 上百分比 | 卷面未要求 | **Active 关** 或删 |
| **RoleImage** | 角色展示图 | 非考点 | **Active 关**（P+1 可留） |
| **BtnMute** | 右上角静音 | Exam04 卷面 **无静音考点** | **Active 关** 或删 · **脚本不绑** |
| **旧 LoadingController** | Prefab 内 Missing/Exam06 脚本 | 跨套污染 | **Remove** · 改挂 **Exam04 `LoadingController`** |

**Hierarchy 目标（Unpack 差量改后）**

```
Exam04_LoadingScene
├── Main Camera
├── EventSystem
├── LoadingCanvas          ← 自 Exam06/05 Prefab Unpack
│   └── LoadingPanel
│       ├── BackGround
│       ├── ProgressSlider
│       └── TipTextTop     ← 其余 Text/BtnMute/RoleImage 已关
└── LoadingManager         ← 空物体 · 挂 LoadingController（或挂 Canvas 根）
```

---

### 3.1 场景挂载（Unpack · **禁止手搭 Canvas**）

打开 **`Exam04_LoadingScene.unity`**。

| # | 操作 |
|:-:|------|
| 1 | Project 拖 **`Assets/Exams/Exam06/Prefabs/LoadingCanvas.prefab`** 进 Hierarchy（Exam05 同路径亦可） |
| 2 | 选中实例 → **Unpack Completely** |
| 3 | 按 **3.0b 体检表** 差量改：关 BtnMute / 多余 Text / RoleImage · Remove 旧脚本 |
| 4 | 若无 **EventSystem**：UI → Event System |
| 5 | 空物体 **`LoadingManager`**（或直接用 **LoadingCanvas** 根）· 第 3.2 步挂 **`LoadingController.cs`** |

#### `LoadingCanvas` · Canvas + Canvas Scaler

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Render Mode | Screen Space - Overlay | UI 最前层 |
| UI Scale Mode | Scale With Screen Size | 按设计分辨率缩放 |
| Reference Resolution | **1920 × 1080** | 与 Login 一致 |
| Match | **0.5** | 宽高折中 |

#### `BackGround` · Image

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Anchor | Stretch 全屏 (0,0)–(1,1) | 铺满 |
| Sprite | Prefab 自带 `BG.png` 即可 | 王者风底图 |
| Raycast Target | **关** | 不挡 Slider |

#### `ProgressSlider` · Slider

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Min / Max | **0 / 1** | 归一化进度 |
| Value | **0** | 初始空 |
| Whole Numbers | **关** | 平滑填充 |
| Interactable | **关** | **脚本驱动** · 玩家不可拖 |
| Handle Slide Area | **无 Handle** 或 Disable | 加载条常见无滑块 |

| RectTransform | Prefab 默认即可 · 若错位再 **Reset** 后只调 Anchor，**禁止**照抄别的场景 Pos Y |

#### `TipTextTop` · Text

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Text | **加载中…** | 占位 · P+1 可 `加载中 {0}%` |
| Raycast Target | **关** | 不挡点击 |

#### 脚本接线表（3.2 步 Inspector）

| SerializeField | 拖谁 |
|----------------|------|
| `progressSlider` | **ProgressSlider** |
| `duration` | **5**（默认 5f 可不改） |

> **P+1**：可选 `[SerializeField] Text tipTextTop` · 协程里 `tipTextTop.text = $"加载中 {Mathf.RoundToInt(slider.value*100)}%"` — 卷面 L1 **不要求**。

---

### 3.2 LoadingController

#### 本段思路

1. **Start** → 存句柄 → `StartCoroutine(LoadProgress())`（**禁止**仅 OnEnable · 踩坑 **L3**）。  
2. `elapsed += Time.deltaTime` · `progressSlider.value = elapsed / duration`。  
3. 满 **5 秒** → `SceneManager.LoadScene(SceneNames.Main)`。  
4. **OnDestroy** → `StopCoroutine(_loadRoutine)` 对称解绑。

#### 带注释 P-L1 范本

```csharp
using System.Collections;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 Loading：5 秒假进度条，满后进入 Main。
    /// [继承 §七] Start 启协程 · yield 刷 UI。
    /// [递进] 纯时间驱动 5s · 无 Exam06 Async/Min · 对称 StopCoroutine。
    /// </summary>
    public class LoadingController : MonoBehaviour
    {
        [SerializeField] private Slider progressSlider;
        [SerializeField] private float duration = 5f;

        // [命名 N1] 私有非序列化字段 _camelCase
        private Coroutine _loadRoutine;

        private void Start()
        {
            progressSlider.value = 0f;
            _loadRoutine = StartCoroutine(LoadProgress());
        }

        /// <summary>思路：每帧 elapsed 累加，Slider 0→1，满 duration 跳转 Main。</summary>
        private IEnumerator LoadProgress()
        {
            float elapsed = 0f;
            while (elapsed < duration)
            {
                elapsed += Time.deltaTime;
                progressSlider.value = Mathf.Clamp01(elapsed / duration);
                yield return null;
            }
            SceneManager.LoadScene(SceneNames.Main);
        }

        private void OnDestroy()
        {
            if (_loadRoutine != null)
                StopCoroutine(_loadRoutine);
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `namespace Exam.Exam04` | **N1** · 与 Auth/SceneNames 同套隔离 |
| `_loadRoutine` | **N1** 私有字段前缀 · 供 OnDestroy 对称 Stop |
| `Start` 启协程 | **递进底线** · 禁 OnEnable-only（Exam06 L3 踩坑） |
| `elapsed / duration` | 卷面 **5s 假进度** · 非 Exam05/06 `Min(fake, real/0.9f)` |
| `SceneNames.Main` | 与第 2 步 **SceneNames** 类一致 · 禁散落字符串 |
| `OnDestroy` + Stop | **P-L1 对称** · 场景切走时不泄漏协程 |

#### Inspector 接线（挂 **LoadingManager** 或 **LoadingCanvas** 根）

| SerializeField | 拖谁 | 含义 |
|----------------|------|------|
| `progressSlider` | Hierarchy **ProgressSlider** | 协程只写 `.value` |
| `duration` | Inspector 填 **5** | 卷面 5 秒 · 可不改默认 5f |

| # | 操作 |
|:-:|------|
| 1 | 若已有空模板 `LoadingController.cs` → **整文件替换**为上范本 |
| 2 | **LoadingManager**（或 Canvas 根）挂脚本 |
| 3 | 拖 **ProgressSlider** → `progressSlider` |
| 4 | Play：**从 Login 链**测 · 勿只开 Loading 场景裸测（无上下文也可 L1 看 Slider） |

#### 读码校准（仓库现状 · 2026-07-06）

| 文件/场景 | 现状 | 本步目标 |
|-----------|------|----------|
| `LoadingController.cs` | 空模板 Start/Update | 按上范本 **整文件重写** |
| `Exam04_LoadingScene` | 已有 **LoadingManager** + **LoadingCanvas** 实例 | 确认 **Unpack** · 关多余 UI · 接线 Slider |

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| Login→Loading→**≈5 秒**→Main · Slider 0→1 | **Prefab Unpack** · **Start 协程** · **StopCoroutine** · **SceneNames.Main** · **N1 `_loadRoutine`** |

### 本步验收

- [ ] **自 Exam06/05 `LoadingCanvas.prefab` Unpack** · **非** UI→Canvas 手搭  
- [ ] Login → Loading → **≈5 秒** → Main（Main 可先空）  
- [ ] Slider 0→1 平滑 · **Interactable 关**  
- [ ] **Start 协程** + **OnDestroy StopCoroutine** · **非** OnEnable-only  
- [ ] `LoadingController` **`namespace Exam.Exam04`** · 无 Missing Script

### 📌 原理加餐（A）

- **是什么**：Loading = **场景切换缓冲区** + **进度反馈**；本卷用 **时间假进度** 满足考点 3，不是真 Async 加载。  
- **U03**：`yield return null` = 等下一帧再刷 Slider，UI 才平滑。  
- **卷面为何**：Login 与 Main 之间 **必须** 有 Loading 场景（考点 3 独立计分）。  
- **vs Exam06**：Exam06 用 **6s + Min(fake, async/0.9f)**；Exam04 **只要 5s 纯 elapsed** — P+1 可在注释里写对照。

### 🔷 工程进阶 B

**行业依据** — 手游 Loading 常 **Max(假进度, 真加载进度)** · 本卷 L1 只验假进度

**FICM · Loading 包（本步）**

| 维 | 试卷 L1 | Exam06 你 | **Exam04 P-L1** |
|:--:|---------|-----------|-----------------|
| ① 职责 | 进度条+跳转 | LoadingController 大包 UI+BGM | **只驱动 Slider + LoadScene** |
| ② 数据 | 5s 满 | duration 6f + Async op | **duration=5f 纯时间** |
| ③ UI | Slider 0→1 | Prefab 全套 | **复用 Prefab · 差量关静音/多余 Text** |
| ⑤ 状态 | 满进 Main | allowSceneActivation | **`SceneNames.Main` 常量** |
| ⑥ 生命周期 | 协程 | Start 协程 · Stop 部分缺 | **Start + `_loadRoutine` + OnDestroy Stop** |
| ⑧ 可维护 | 可读 | 字面量场景名 | **SceneNames** · **N1 命名** |

**B本步 · 对照要点**

- **假进度** = 行业 **时间遮罩**；真项目再叠 **LoadSceneAsync.progress**。  
- **Prefab 复用** = 少重复搭 UI · 本步 **G1 门禁** 强制。

**B下一档（P+1）**：`[SerializeField] Text tipTextTop` 刷 `%` · 注释 **Min(fake, real/0.9f)** 对照 Exam06。

### 进度（第 3 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 4 步：Main 移动 + 动画 + 刷怪（考点 4～5）· v7.16.8 读码重生成

> 状态：**🔒 ← 当前**  
> **§11.29 递进**：Exam03 移动 + **`SetBool("IsWalking")`**（对齐仓库 **Master.controller**）· 敌人 **直接用 Enemy.controller 默认 Idle**  
> **禁止**：新建 `PlayerAnimator.controller` · 参数名 **`IsMoving`**（与仓库不一致）

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| **4.0** | **Animator 读码对照表** | 参数与 Controller 一致 |
| **4.1** | Plane · Player/Enemy Prefab · GameManager | 场景可见 |
| **4.2** | `PlayerController` · `GameManager` | 动停 Walk/Idle · 3 敌 Idle |

#### 本段思路（整步）

**不搭状态机** — 你已有 **`Assets/Animator/Master.controller`**（Player）与 **`Enemy.controller`**（Enemy Prefab 已挂）。本步只写 **移动 + `IsWalking` Bool** + **刷 3 敌**；**Attack / Die / Skill** 留第 5 步。

---

### 4.0 Animator 读码对照（Read 仓库 · 本步必用参数）

#### Player · `Master.controller`（`Exam04/Prefabs/Player.prefab` 已挂）

| 参数 | 类型 | 本步是否用 | 驱动方式 |
|------|------|:----------:|----------|
| **`IsWalking`** | **Bool** | ✅ | `SetBool` · Idle↔Walk |
| `Attack` | Trigger | ❌ 第 5 步 | 开火钮 `SetTrigger` |
| `Die` | Trigger | ❌ | — |
| `SkillU` / `SkillI` | Trigger | ❌ P+1 | — |

| 状态 | 说明 |
|------|------|
| **Idle** | 默认 · `IsWalking == false` |
| **Walk** | `IsWalking == true` |
| Attack / Die / Skill* | 第 5 步及以后 |

#### Enemy · `Enemy.controller`（`Exam04/Prefabs/Enemy.prefab` 已挂）

| 参数 | 类型 | 本步是否用 | 说明 |
|------|------|:----------:|------|
| **`IsWalking`** | Bool | ❌ 本步 | 刷出后 **保持默认 Idle** 即可 |
| `Attack` | Trigger | ❌ P+1 | — |
| **`Die`** | Trigger | ❌ **第 5 步** | 受击死亡 `SetTrigger("Die")` |

| 状态 | 本步 |
|------|------|
| **Idle** | **默认状态** · Instantiate 后自动播 · **无需脚本** |
| Walk / Attack / Die | 第 5 步战斗 |

---

### 4.1 Main 场景摆放（Prefab 优先 · 不新建 Controller）

打开 **`Exam04_MainScene.unity`**（你已有 Plane / Player / GameManager 可核对）。

| # | 操作 |
|:-:|------|
| 1 | 拖 **`Exam04/Prefabs/Plane.prefab`**（或 Exam03 Plane）→ **(0,0,0)** |
| 2 | 拖 **`Exam04/Prefabs/Player.prefab`** → 确认 **Animator → Controller = `Master`** |
| 3 | Player 挂 **`PlayerController.cs`** · Inspector 拖 **Animator** |
| 4 | **`GameManager`** 挂 **`GameManager.cs`** |
| 5 | **`GameManager.enemyPrefab`** → **`Exam04/Prefabs/Enemy.prefab`**（已含 **Enemy** Controller） |

**Hierarchy 目标**

```
Exam04_MainScene
├── Main Camera
├── Directional Light
├── Plane
├── Player          (Master.controller + PlayerController)
├── GameManager     (GameManager)
└── （运行时 3× Enemy · 默认 Idle）
```

**禁止本步**

- ❌ 新建 `PlayerAnimator.controller`  
- ❌ 在代码里写 **`IsMoving`**（Master 里是 **`IsWalking`**）  
- ❌ 给敌人写移动逻辑（卷面第 4 步只要 **待机 Idle**）

---

### 4.2 PlayerController + GameManager

#### PlayerController · 本段思路

1. WASD → `Translate` / `Rotate`（Exam03 `TrankController` · **Space.Self**）。  
2. `moving = |h|+|v| > ε` → **`animator.SetBool("IsWalking", moving)`**（对齐 **Master.controller**）。

#### 带注释 P-L1 范本

```csharp
using UnityEngine;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 主角：WASD 移动 + IsWalking 驱动 Master.controller Idle/Walk。
    /// [继承 §七] Exam03 TrankController · Space.Self。
    /// [递进] 参数名 IsWalking（非 IsMoving）· 对齐仓库 Master.controller。
    /// </summary>
    public class PlayerController : MonoBehaviour
    {
        [SerializeField] private float moveSpeed = 6f;
        [SerializeField] private float turnSpeed = 90f;
        [SerializeField] private Animator animator;

        private static readonly int IsWalkingHash = Animator.StringToHash("IsWalking");

        private void Update()
        {
            float h = Input.GetAxis("Horizontal");
            float v = Input.GetAxis("Vertical");

            transform.Translate(new Vector3(0, 0, v) * moveSpeed * Time.deltaTime, Space.Self);
            transform.Rotate(0f, h * turnSpeed * Time.deltaTime, 0f);

            bool moving = Mathf.Abs(h) > 0.01f || Mathf.Abs(v) > 0.01f;
            animator.SetBool(IsWalkingHash, moving);
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `IsWalkingHash` | 与 **Master.controller** 参数名 **完全一致** · 禁写 `IsMoving` |
| `SetBool` | Bool 驱动 Idle↔Walk · **Trigger 本步不用** |
| `Space.Self` | 相对自身前后 · Exam03 同款 |

#### Inspector 接线

| SerializeField | 拖谁 |
|----------------|------|
| `animator` | Player 根上 **Animator** 组件 |

---

#### GameManager · 本段思路

Start → 循环 3 次 **Instantiate(Enemy.prefab)** · XZ Random · 敌人 **Animator 默认 Idle** 自动播。

```csharp
using UnityEngine;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 主场景：随机刷 3 敌人。Enemy.prefab 已挂 Enemy.controller · 默认 Idle。
    /// [继承 §七] Exam03 GameController 刷怪。
    /// [递进] aliveCount / Die Trigger 在第 5 步。
    /// </summary>
    public class GameManager : MonoBehaviour
    {
        [Header("刷怪")]
        [SerializeField] private GameObject enemyPrefab;
        [SerializeField] private int enemyCount = 3;
        [SerializeField] private float spawnRangeX = 22.5f;
        [SerializeField] private float spawnRangeZ = 22.5f;
        [SerializeField] private float spawnY = 0.35f;

        private void Start()
        {
            SpawnEnemies();
        }

        /// <summary>思路：循环 enemyCount 次 Instantiate · 敌人无需脚本即 Idle。</summary>
        private void SpawnEnemies()
        {
            if (enemyPrefab == null) return;

            for (int i = 0; i < enemyCount; i++)
            {
                float x = Random.Range(-spawnRangeX, spawnRangeX);
                float z = Random.Range(-spawnRangeZ, spawnRangeZ);
                Vector3 pos = new Vector3(x, spawnY, z);
                Instantiate(enemyPrefab, pos, Quaternion.identity);
            }
        }
    }
}
```

#### Inspector 接线

| SerializeField | 拖谁 |
|----------------|------|
| `enemyPrefab` | **`Exam04/Prefabs/Enemy.prefab`** |

#### 读码校准（仓库 · 2026-07-06）

| 项 | 现状 | 本步 |
|----|------|------|
| `PlayerController.cs` | 空模板 | 按上范本重写 · **`IsWalking`** |
| `GameManager.cs` | 空模板 | 按上范本重写 |
| Main 场景 | Plane/Player/GameManager 已有 | 补脚本 + 拖引用 |
| Player Animator | **Master.controller** | **勿改 Controller** |
| Enemy Prefab | **Enemy.controller** · 默认 Idle | **勿改** · 第 5 步 `Die` Trigger |

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| WASD · 站 Idle / 走 Walk · 3 敌 **Idle** | **`IsWalking` SetBool** · **Master/Enemy Controller 直接用** · **Space.Self** |

### 本步验收

- [ ] **未新建** Animator Controller · Player = **Master** · Enemy = **Enemy**  
- [ ] 站定 **Idle** / 移动 **Walk**（Master 状态名）  
- [ ] **3 敌人** 随机位置 · **Idle 循环**  
- [ ] 代码参数 **`IsWalking`** · 无 `IsMoving`

### 📌 原理加餐（A）

- **U07**：Bool **IsWalking** 与状态机转移条件一致；**Trigger**（Attack/Die）一次脉冲，第 5 步再用。  
- **敌人 Idle**：Default State = Idle → **Instantiate 即播**，不必 `animator.Play("Idle")`。

### 🔷 工程进阶 B

| 维 | 讲义旧版 | **读码后 Exam04** |
|:--:|----------|-------------------|
| Player 参数 | `IsMoving` | **`IsWalking` · Master.controller** |
| Enemy 动画 | 手建单 Idle | **Enemy.controller 已有 Idle/Walk/Attack/Die** |
| 第 5 步预留 | — | Player **`Attack` Trigger** · Enemy **`Die` Trigger** |

**B下一档（P+1）**：敌人 `IsWalking` 巡逻 · Master `SkillU/SkillI` 技能钮。

### 进度（第 4 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 5 步：战斗 UI + 子弹 + 全灭胜利（考点 6～9）

> 状态：**🔒 待学员完成**  
> **§11.29 递进**：禁 **FindObjectOfType** · **`aliveCount`** · mute **双反馈**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| 5.1 | PlayerPanel 三钮 UI | 音乐/开火/关闭 |
| 5.2 | Bullet · Enemy 死亡 · GameManager 胜利 | 全灭 Text |

---

### 5.1 主角面板 UI

| 物体 | 说明 |
|------|------|
| `PlayerPanel` | Screen Space Overlay 子 Canvas 或 World Space |
| `BtnMusic` | 一键 mute · **Text「音乐:开/关」** 双反馈 |
| `BtnFire` | Instantiate Bullet |
| `BtnClose` | `playerPanel.SetActive(false)` **只关面板** |

**禁止**：两个钮分别开/关音乐。

---

### 5.2 脚本思路

#### GameManager

1. `SpawnEnemies` 时 `aliveCount = 3` · 每个敌人 **`Init(this)`** 注入。  
2. **`OnEnemyDied()`** → `aliveCount--` · 0 时 **`victoryText.SetActive(true)`**。

#### EnemyController（递进 vs Exam03）

- **禁止** `FindObjectOfType`  
- **`[SerializeField] int hp`** · 子弹 Trigger → hp-- → 0 时 **`gameManager.OnEnemyDied()`** + Destroy self

#### BattleUIController

- mute 切换 + **更新 Text**  
- Fire → Instantiate + AddForce  
- Close → 隐藏 Panel  
- **OnDestroy** 解绑

```csharp
// BattleUIController · mute 双反馈片段
private void ToggleMusic()
{
    isMuted = !isMuted;
    bgmSource.mute = isMuted;
    musicStateText.text = isMuted ? "音乐:关" : "音乐:开";
}
```

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| 三钮 · 子弹 · 全灭胜利 | **注入 GameManager** · **aliveCount** · **双反馈** · **OnDestroy** |

### 本步验收

- [ ] 音乐一键 + **文案变化**  
- [ ] 开火有子弹 · 敌人 Destroy · **aliveCount 减**  
- [ ] 关闭只藏 Panel · 游戏继续  
- [ ] **3 敌全灭** → 胜利 Text  
- [ ] 代码 **无** FindObjectOfType

### 📌 原理加餐（A）

- **U19**：胜负 = **计数器** · 与 Exam03 **加血胜利** 不同 — 卷面别混。  
- **U10**：子弹 Instantiate — Exam03 已有 · 本套加 **敌人死亡回调**。

### 🔷 工程进阶 B

**FICM · 战斗包**

| 维 | Exam03 你现状 | **Exam04 P-L1** |
|:--:|---------------|-----------------|
| ② 数据 | FindObjectOfType 加血 | **SerializeField 注入 GameManager** |
| ⑤ 状态 | 加血满条胜利 | **aliveCount == 0** |
| ③ UI | 开火钮 | + **mute 双反馈** + 关闭 Panel |

**模块完成**：Play 跑通后 Coach 问 **`小灶：Exam04战斗`** 或 **`跳过小灶`**（§11.24）。

### 进度（第 5 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 6 步：注释 + Build 断网交付（考点 10）

> 状态：**🔒 待学员完成**

### 6.1 §11.19 注释自检

| # | 检查 |
|:-:|------|
| 1 | 每类 **类顶 summary** + 方法 **思路注释** |
| 2 | 字段 **`[Header]` 分区** |
| 3 | 删 **unused using**（如 VisualScripting） |
| 4 | 全脚本 **`namespace Exam.Exam04`** |

### 6.2 Build 断网

| # | 项 |
|:-:|-----|
| 1 | Build **仅 Exam04** 三场景 **0→1→2** |
| 2 | 断网全流程 Play |
| 3 | `Exam04_思维导图.png` → `Docs/` |
| 4 | Chat **「Exam04 交付完成」** |

### 本步验收

- [ ] 断网无报错  
- [ ] **§11.19** 全脚本过  
- [ ] 递进项 **§一点五 v3** 全 ✅

### 📌 原理加餐（A）

- **U16**：工程隔离 + 可读性 = 主程基本功。

### 🔷 工程进阶 B

**B本步**：交付清单 = CI 最小门禁 · 行业 Build 还有 **Script Compilation + 资源校验**（阶段 02）。

### 进度（第 6 步）

| L1 | P-L1 | 备注 |
|:--:|:----:|------|
| ☐ | ☐ | 交付 |

---

## 断网后 · 档案六步（§11.7）

> **「Exam04 交付完成」** 或 **开 Exam05** → Coach 同一轮写入四文件。

## 当前进度表

| 大步 | L1 | P-L1 | A | B | 备注 |
|------|:--:|:----:|:-:|:-:|------|
| **第 4 步** | ☐ | ☐ | ☐ | ☐ | **← 当前** |
| 第 3 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 2 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 1 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 4 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 5 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 6 步 | ☐ | ☐ | — | — | 交付 |
